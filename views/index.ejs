<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lux - モダンなライフスタイルマガジン</title>
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300;400&display=swap">
</head>
<body>
    <div class="loading-overlay">
        <div class="sk-cube-grid">
            <div class="sk-cube sk-cube1"></div>
            <div class="sk-cube sk-cube2"></div>
            <div class="sk-cube sk-cube3"></div>
            <div class="sk-cube sk-cube4"></div>
            <div class="sk-cube sk-cube5"></div>
            <div class="sk-cube sk-cube6"></div>
            <div class="sk-cube sk-cube7"></div>
            <div class="sk-cube sk-cube8"></div>
            <div class="sk-cube sk-cube9"></div>
        </div>
    </div>

    <%- include('partials/header') %>

    <main>
        <section class="hero type" id="type01">
            <div class="hero-content">
                <h1>グラスが紡ぐ、美しい物語</h1>
                <p>Luxは、グラスという美しい器を通して、人生の豊かさを探求するメディアです。</p>
            </div>
            <a href="#content">Scroll</a>
        </section>

        <div id="content">
            <section class="featured-post">
                <% if (posts.length > 0) { %>
                    <article class="post-card featured">
                        <img src="<%= posts[0].image %>" alt="<%= posts[0].title %>" class="post-image">
                        <div class="post-content">
                            <h2 class="post-title"><%= posts[0].title %></h2>
                            <div class="post-date"><%= posts[0].date %></div>
                            <p class="post-excerpt"><%= posts[0].excerpt %></p>
                            <a href="/posts/<%= posts[0].slug %>" class="read-more">続きを読む</a>
                        </div>
                    </article>
                <% } %>
            </section>

            <section class="recent-posts">
                <h2>最新の記事</h2>
                <div class="posts-grid">
                    <% posts.slice(1).forEach(function(post) { %>
                        <article class="post-card">
                            <img src="<%= post.image %>" alt="<%= post.title %>" class="post-image">
                            <div class="post-content">
                                <h3 class="post-title"><%= post.title %></h3>
                                <div class="post-date"><%= post.date %></div>
                                <p class="post-excerpt"><%= post.excerpt %></p>
                                <a href="/posts/<%= post.slug %>" class="read-more">続きを読む</a>
                            </div>
                        </article>
                    <% }); %>
                </div>
            </section>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script>
        // ローディングアニメーション
        window.addEventListener('load', () => {
            const loadingOverlay = document.querySelector('.loading-overlay');
            setTimeout(() => {
                loadingOverlay.style.opacity = '0';
                setTimeout(() => {
                    loadingOverlay.style.display = 'none';
                }, 500);
            }, 2000);
        });

        // スムーススクロール
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>